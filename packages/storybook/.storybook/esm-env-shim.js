// Simple runtime shim for Svelte's `esm-env` imports when bundler replacement is unavailable
// Provides reasonable dev-time defaults for Storybook preview.
export const DEV = true;
export const PROD = false;
export const BROWSER = true;
